import { useEffect } from "react";


function MNavbar(props){

    return (
        <div className={`${props.width?'w-60v':'w-0'} bg-black h-screen absolute overflow-hidden transition-all transform origin-right`}>hii</div>
    )
}
export default MNavbar;